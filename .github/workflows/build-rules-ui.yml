name: Build Rules UI and Push to Harbor

on:
  workflow_dispatch:
  
env:
  REGISTRY: harbor.bnov.net
  PROJECT: gmail
  APP_NAME: gmail-project-rules-ui
  FULL_IMAGE: harbor.bnov.net/gmail/gmail-project-rules-ui

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Harbor
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.HARBOR_USERNAME }}
          password: ${{ secrets.HARBOR_PASSWORD }}


      - name: Build and push (multi-arch)
        uses: docker/build-push-action@v5
        with:
          context: ./email-processor/rules-ui
          file: ./email-processor/rules-ui/Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ${{ env.FULL_IMAGE }}:build-${{ github.run_number }}
            ${{ env.FULL_IMAGE }}:latest